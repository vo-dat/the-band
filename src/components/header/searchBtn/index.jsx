import React from 'react';
import PropTypes from 'prop-types';
import './searchBtn.css'
SearchBtn.propTypes = {
    
};

function SearchBtn(props) {
    return (
      
            <div className="search-icon ti-search"></div>
       
    );
}

export default SearchBtn;